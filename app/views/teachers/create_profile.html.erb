<h1>Set your DemoLesson profile name</h1>

<p>This is the unique name you'll have on the site, e.g. <strong>http://demolesson.com/yourname</strong>. 

<%= form_for @teacher, :as => :teacher, :url => '/teachers/'+@teacher.id.to_s, :html => { :multipart => true } do |f| %>
  <% if @teacher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@teacher.errors.count, "error") %> prohibited this teacher from being saved:</h2>

      <ul>
      <% @teacher.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :url %>
  </div>

<div class="actions">
    <%= f.submit "Submit" %>
  </div>

<% end %>