<h1>Submitting Application</h1>

Please enter your TFA Application Passcode. Once you click submit, <%= link_to @job.school.name, @job.school %> will be able to view your profile, video, and attachments.

<%= form_for @job, :as => :job, :url => "/tfa_apply/"+@job.id.to_s do |f| %>
  <div class="field">
	<%= f.text_field :passcode, :value => '' %>
  </div>

  <div class="actions">
  	<%= f.submit "Submit" %>
  </div>
<% end %>