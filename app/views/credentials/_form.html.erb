<%= form_for(@credential) do |f| %>
  <% if @credential.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@credential.errors.count, "error") %> prohibited this credential from being saved:</h2>

      <ul>
      <% @credential.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>

  <p class="field">
    <%= f.label :credentialType %><br />
    <%= f.text_field :credentialType %>
  </p>

  <p class="field">
    <%= f.label :issuer %><br />
    <%= f.text_field :issuer %>
  </p>

  <p class="actions">
    <%= f.submit %>
  </p>
<% end %>
