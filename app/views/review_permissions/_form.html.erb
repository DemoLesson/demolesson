<%= form_for(@review_permission) do |f| %>
  <% if @review_permission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@review_permission.errors.count, "error") %> prohibited this review_permission from being saved:</h2>

      <ul>
      <% @review_permission.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :job_id %><br />
    <%= f.text_field :job_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
